import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  card: {
    width: 70,
    marginRight: 10,
    marginTop: 0,
    alignItems: 'center',
  },
  poster: {
    width: '100%',
    height: 70,
    borderRadius: 55,
    marginBottom: 8,
  },
});
